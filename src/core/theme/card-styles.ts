/**
 * Dashboard card corner multiplier for `sx.borderRadius`.
 * Matches the primary KPI card (e.g. Total beneficiaries reached): `1 × theme.shape.borderRadius`.
 */
export const CARD_BORDER_RADIUS_SX = 1 as const
